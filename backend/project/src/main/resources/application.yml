spring:
  application:
    name: sample

  profiles:
    include: dev
  servlet:
    multipart:
      max-request-size: 10MB
      max-file-size: 10MB

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

#  data:
#    redis:
#      host: {REDIS_HOST}
#      port: {REDIS_PORT}
#      password: {REDIS_PASSWORD}

server:
  port: 8081
  servlet:
    context-path: /api

logging:
  level:
    com:
      sk:
        skala:
          axcalibur: DEBUG